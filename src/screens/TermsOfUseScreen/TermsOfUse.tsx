import { StyleSheet, Text, View } from "react-native";
import React, { ReactElement } from "react";

interface PropTypes {}

export default function TermsOfUse({}: PropTypes): ReactElement {
  return (
    <View>
      <Text>TermsOfUse</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
