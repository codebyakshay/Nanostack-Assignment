import { StyleSheet, Text, View } from "react-native";
import React, { ReactElement } from "react";

interface PropTypes {}

export default function LeaveRequest({}: PropTypes): ReactElement {
  return (
    <View>
      <Text>LeaveRequest</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
