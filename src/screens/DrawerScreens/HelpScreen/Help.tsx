import { StyleSheet, Text, View } from "react-native";
import React, { ReactElement } from "react";

interface PropTypes {}

export default function Help({}: PropTypes): ReactElement {
  return (
    <View>
      <Text>Help</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
