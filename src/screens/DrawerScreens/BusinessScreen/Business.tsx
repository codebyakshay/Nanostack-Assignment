import { StyleSheet, Text, View } from "react-native";
import React, { ReactElement } from "react";

interface PropTypes {}

export default function Business({}: PropTypes): ReactElement {
  return (
    <View>
      <Text>Business</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
