import { StyleSheet, Text, View } from "react-native";
import React, { ReactElement } from "react";

interface PropTypes {}

export default function Security({}: PropTypes): ReactElement {
  return (
    <View>
      <Text>Security</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
