import { StyleSheet, Text, View } from "react-native";
import React, { ReactElement } from "react";

interface PropTypes {}

export default function Subscription({}: PropTypes): ReactElement {
  return (
    <View>
      <Text>Subscription</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
