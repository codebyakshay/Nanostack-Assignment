import { StyleSheet, Text, View } from "react-native";
import React, { ReactElement } from "react";

interface PropTypes {}

export default function PrivacyPolicy({}: PropTypes): ReactElement {
  return (
    <View>
      <Text>PrivacyPolicy</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
