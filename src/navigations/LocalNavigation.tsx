import { Text, View } from "react-native";
import React, { ReactElement } from "react";

interface PropTypes {}

export default function LocalNavigation({}: PropTypes): ReactElement {
  return (
    <View>
      <Text>LocalNavigation</Text>
    </View>
  );
}
